@namespace HtmxBlazor.Components
@inherits LayoutComponentBase

@Body

@*
    Empty layout for routable fragment components (components with an @page route
    that htmx fetches). Prevents the fragment from being wrapped in the full
    HTML document of the default layout. Apply with: @layout HxFragmentLayout
*@
